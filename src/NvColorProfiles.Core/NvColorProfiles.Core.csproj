<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>nv_color_profiles.core</RootNamespace>
    <AssemblyName>NvColorProfiles.Core</AssemblyName>
    <!-- Required for NVAPI interop: fixed buffers, function pointers, raw struct pointers. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.7" /> <!-- Pinned; supply-chain cooldown -->
    <!-- NvAPI .NET wrapper for vibrance/hue/display enumeration. -->
    <PackageReference Include="NvAPIWrapper.Net" Version="0.8.1.101" /> <!-- Pinned; supply-chain cooldown -->
  </ItemGroup>

</Project>
